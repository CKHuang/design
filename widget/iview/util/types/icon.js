export default {
    type(
        output = 'option'
    ) {
        const icons = [
            {value:`ios-add`,label:`ios-add`}
        ]
        if (output == 'option') {
            return icons;
        }
    }
}