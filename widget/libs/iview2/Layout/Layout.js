import Widget from '../../../base/Widget'

export default class Layout extends Widget {
    constructor() {
        super(
            `Layout`,
            `iview`,
            `Layout`
        )
    }
}