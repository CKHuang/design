import Widget from '../../../base/Widget'

export default class Header extends Widget {
    constructor() {
        super(
            `Header`,
            `iview`,
            `Header`
        )
    }
}